<UserControl x:Class="Financisto.Common.Filters.StartYearMonthFilter"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fcrd="using:Financisto.Common.Entities"
             xmlns:loc="using:Financisto.Common.Localization"
             Width="120">
  <StackPanel>
    <TextBlock Text="{loc:Translate Key=from_}" FontSize="12" FontWeight="SemiBold" Margin="2 0 2 2"/>
    <ComboBox Margin="2 0"
              IsEditable="True"
              DisplayMemberBinding="{Binding Name}"
              SelectedItem="{Binding Path=StartYearMonths}"
              ItemsSource="{Binding Source={x:Static fcrd:DbManual.YearMonths}, Mode=OneWay}" />
  </StackPanel>
</UserControl>
